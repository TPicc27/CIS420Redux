
@using DayPilot.Web.Mvc.Events.Month

@section TitleContent { Monthly Calendar Event Customization }

<script type="text/javascript">

	function createEvent(start, end) {
	    var name = prompt("New Event Name:", "New Event");
	    if (!name) { return; }
	    dp.timeRangeSelectedCallBack(start, end, { name: name });
	    dp.clearSelection();
	}

</script>

<div class="note"><b>Note:</b> See more about <a href="https://doc.daypilot.org/month/event-customization/">event customization</a>.</div>

@(Html.DayPilotMonth("dp_customization", new DayPilotMonthConfig
{ 
    BackendUrl = Url.Content("~/Month/Backend"),
                
    TimeRangeSelectedHandling = TimeRangeSelectedHandlingType.JavaScript,
    TimeRangeSelectedJavaScript = "createEvent(start, end)",
    EventMoveHandling = EventMoveHandlingType.CallBack,
    EventResizeHandling = EventResizeHandlingType.CallBack
}))



